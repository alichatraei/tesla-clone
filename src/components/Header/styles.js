import { StyleSheet } from "react-native";
export default styles = StyleSheet.create({
  logoContainerStyles: {
    position: "absolute",
    top: 50,
    paddingHorizontal: 20,
    zIndex: 100,
    borderWidth: 10,
  },
  logoStyles: {
    width: 100,
    height: 150,
    resizeMode: "contain",
    position: "absolute",
    left: 40,
    zIndex: 12,
    top: 0,
  },
});
